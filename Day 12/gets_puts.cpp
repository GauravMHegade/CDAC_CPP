#include<iostream>
#include<stdlib.h>
using namespace std;
int main()
{
    char str[20];
    cout<<"Enter string : "<<endl;
    gets(str); //for accepting string with spaces
    puts(str);
}